<p id="notice"><%= notice %></p>

<h1>Companies</h1>

<table class="table">
  <thead>
    <tr>
      <th>Name</th>
      <th>Country</th>
      <th>State</th>
      <th>City</th>
      <th>Postcode</th>
      <th>Start date</th>
      <th>Active</th>
    </tr>
  </thead>

  <% @companies.each do |company| %>
        <td><%= company.name %></td>
         <td><%if company.country_id.present? %> 
          <%= Country.find(company.country_id).country_name %> 
           <% end %> </td>
         <td><%if company.state_id.present? %> 
        <%= State.find(company.state_id).state_name %> 
    <% end %> </td>
         <td> <%if company.city_id.present? %> 
     <%= City.find(company.city_id).city_name %> 
    <% end %> </td>
        <td><%= company.postcode %></td>
         <td><%= company.start_date %></td>
         <td><%= company.active %></td>
         <td><%= link_to 'Show', company,:class=>"btn btn-info" %></td>
         <td><%= link_to 'Edit', edit_company_path(company),:class=>"btn btn-warning" %></td>
         <td><%= link_to 'Destroy', company, method: :delete, data: { confirm: 'Are you sure?' },:class=>"btn btn-danger" %></td>
      </tr>
    <% end %>
</table>

<%= link_to 'New Company', new_company_path %>
