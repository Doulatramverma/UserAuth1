<%= form_for user_region,:html=>{:class=>"form-horizontal"} do |f| %>
  <% if user_region.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_region.errors.count, "error") %> prohibited this user_region from being saved:</h2>

      <ul>
      <% user_region.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  

  <div class="form-group">
        <label class="col-sm-2 control-label">Name</label>
        <div class="col-sm-6  inputGroupContainer">
        <%=f.select(:user_id, User.all.collect {|u| [u.first_name, u.id]},{},:prompt => "Select User",:class=>"form-control") %>
        
          </div>
        </div>

      <div class="form-group">
        <label class="col-sm-2 control-label">Region</label>
        <div class="col-sm-2  inputGroupContainer">
            <%=f.select(:region_id, Region.all.collect {|u| [u.region_name, u.id]},{},:prompt => "Select Region",:class=>"form-control",:multiple=>true) %>
       </div>
       </div>
       
       <div class="form-group">
        <label class="col-sm-2 control-label"></label>
        <div class="col-sm-3  inputGroupContainer">
            <%=text_area,"xyz",:size=>"5x4" %>
       </div>
       </div>


     
  <div class="form-group">
        <label class="col-md-4 control-label"></label>
        <div class="col-md-4">
          <%=f.submit :class=>"btn btn-success" %>
        </div>
      </div>
<% end %>


